from django.urls import path
from api import views

urlpatterns = [
    # TODO: Need to update this with views for CrossReading
    # TODO: And write tests to cover because right now they don't
    #       cover the URL - only the class based view
    # path('cards/', views.GetCards.as_view()),
    # path('cards/<int:pk>/', views.GetCard.as_view()),
]